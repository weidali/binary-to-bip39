# Binary to BIP-39 Mnemonic Generator

Консольный Python-скрипт для преобразования бинарной строки (0 и 1)  
в BIP-39 мнемоническую фразу (seed phrase).

Подходит для:
- оффлайн-генерации сид-фраз
- экспериментов с энтропией
- обучения принципам BIP-39
- аппаратных / DIY-кошельков

---

## ⚠️ ВАЖНО

Используйте скрипт **ТОЛЬКО в оффлайн-среде**, если вы работаете с реальными средствами.  
Автор не несёт ответственности за потерю средств.

---

## 🔢 Поддерживаемые длины энтропии

| Биты | Слова |
|-----|-------|
| 128 | 12 |
| 160 | 15 |
| 192 | 18 |
| 224 | 21 |
| 256 | 24 |

Бинарная строка **должна состоять только из `0` и `1`**.

## 📦 Зависимости

- Python 3.8+
- Linux / macOS: без дополнительных библиотек
- Windows:
  ```bash
  pip install windows-curses

---

## 🚀 Установка

### 1. Клонировать репозиторий
```bash
git clone https://github.com/weidali/binary-to-bip39.git
cd binary-to-bip39
```

## Виртуальное окружение (venv)

Рекомендуется использовать виртуальное окружение Python.

### Linux / macOS
```bash
python3 -m venv venv
source venv/bin/activate

pip install -r requirements.txt

deactivate
```

## Run

```bash
python bip39_from_binary.py
```
